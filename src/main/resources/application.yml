spring:


  # 2. MariaDB 설정 (spring.datasource) -> ★ 위치 수정됨


  # 3. JPA 설정 (MariaDB용)
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
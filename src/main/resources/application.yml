spring:
  profiles:
    active: local

  application:
    name: empasy-graph-api

  # 3. JPA 설정 (MariaDB용 공통 설정)
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true